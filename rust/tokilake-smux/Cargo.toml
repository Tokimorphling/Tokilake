[package]
name = "tokilake-smux"
version = "0.1.0"
description = "Tokilake SMUX - Wire-compatible smux protocol, zero-overhead Rust implementation"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt", "sync", "io-util", "time", "macros"] }
bytes = "1"
tracing = "0.1"
